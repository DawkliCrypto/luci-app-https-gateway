# Compatibility wrapper: some external OpenWrt build systems expect the package
# definition to live under package/<name>/Makefile. The canonical source of truth
# remains the repo-root Makefile so the package metadata is kept in one place.
include $(CURDIR)/../../Makefile
